---
phase: 02-day-scoped-estimate-indicator
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/reports/page.tsx
autonomous: true
requirements:
  - REP-01

must_haves:
  truths:
    - "The estimate indicator sub-row shows only the time tracked for that specific day, not cumulative all-time total"
    - "When a task has logs on multiple days, each day card displays only that day's tracked duration in the indicator"
    - "A task with zero minutes logged on a given day shows 0:00:00 in the indicator, not a carry-forward value"
    - "The percentage color (red/orange/green) and the check/cross icon still reflect all-time progress vs estimate"
  artifacts:
    - path: "app/(app)/reports/page.tsx"
      provides: "Reports page with day-scoped estimate indicator"
      contains: "task.duration"
  key_links:
    - from: "dayMap (day.tasks)"
      to: "estimate indicator sub-row"
      via: "task.duration in JSX at line ~425"
      pattern: "task\\.duration"
---

<objective>
Fix the estimate indicator sub-row in the Reports log to display the day-specific tracked duration instead of the all-time cumulative total.

Purpose: Users see accurate daily progress — how much they worked on a task that day vs the task estimate — rather than a misleading all-time total that makes older tasks appear perpetually overrun.
Output: Updated app/(app)/reports/page.tsx where the indicator sub-row reads `task.duration` instead of `totalTracked`.
</objective>

<execution_context>
@/Users/bogdan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bogdan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope estimate indicator to day-specific duration</name>
  <files>app/(app)/reports/page.tsx</files>
  <action>
    In the task-row render block (inside `taskList.map`), locate the "Estimate indicator row" comment at approximately line 419.

    Currently the indicator sub-row (inside `{estimated !== null && (...)`) renders:

    ```tsx
    {formatDuration(totalTracked)} / {formatDuration(estimated)}
    ```

    Change `totalTracked` to `task.duration` so the displayed tracked value is the day-specific aggregate from `dayMap`, not the all-time total from `allTimeByTask`.

    The result should read:

    ```tsx
    {formatDuration(task.duration)} / {formatDuration(estimated)}
    ```

    DO NOT change:
    - The `ratio` calculation (still uses `totalTracked` for color thresholds — intentional, per REQUIREMENTS.md "Out of Scope")
    - The `isDone` calculation (still uses `totalTracked` — intentional)
    - The `indicatorColor` derivation
    - The `allTimeByTask` query or map (still needed for `estimated`, `isDone`, and `indicatorColor`)
    - Anything in the `copyTasks` block (it already uses `task.duration` for `trackedFormatted`)
    - Any other part of the file

    This is a single token substitution in one JSX expression.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build succeeds with no TypeScript errors. In the Reports page, the estimate indicator sub-row for any task shows the day-specific tracked time (matching the duration shown in the title row), not the all-time total. A task tracked 1h today but 10h historically shows "1:00:00 / estimate", not "10:00:00 / estimate".
  </done>
</task>

</tasks>

<verification>
Run `pnpm build` — must complete with zero errors.

Manual spot-check: open /reports, find a task that has logs on more than one day (or has a large historical total). Confirm the indicator sub-row shows the same duration as the editable field in the title row for that day.
</verification>

<success_criteria>
1. Build passes with no TypeScript errors.
2. Each day card's estimate indicator reflects only that day's tracked time.
3. The ✅/❌ icon and color coding are unchanged (still based on all-time progress).
</success_criteria>

<output>
After completion, create `.planning/phases/02-day-scoped-estimate-indicator/02-01-SUMMARY.md` using the summary template.
</output>
