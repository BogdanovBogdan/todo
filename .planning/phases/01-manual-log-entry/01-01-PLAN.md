---
phase: 01-manual-log-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/actions/time-logs.ts
  - app/(app)/reports/_components/add-time-log-modal.tsx
autonomous: true
requirements:
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05

must_haves:
  truths:
    - "saveTimeLog accepts duration = 0 without silent no-op"
    - "saveTimeLog triggers Reports page refresh after insert"
    - "Modal renders Task, Date, and Duration fields in that order"
    - "Task field filters the task list as the user types"
    - "Date field defaults to today and accepts any date"
    - "Duration field is optional; blank is accepted and saved as 0"
    - "Saving with invalid h:mm shows an inline validation error, not a crash"
    - "On successful save, onClose() is called"
  artifacts:
    - path: "lib/actions/time-logs.ts"
      provides: "saveTimeLog mutation accepting duration >= 0 with revalidatePath"
      contains: "data.duration < 0"
    - path: "app/(app)/reports/_components/add-time-log-modal.tsx"
      provides: "AddTimeLogModal Client Component — 3-field form"
      exports: ["AddTimeLogModal"]
  key_links:
    - from: "app/(app)/reports/_components/add-time-log-modal.tsx"
      to: "lib/actions/time-logs.ts"
      via: "saveTimeLog server action call inside startTransition"
      pattern: "saveTimeLog"
    - from: "app/(app)/reports/_components/add-time-log-modal.tsx"
      to: "lib/utils/tz.ts"
      via: "dateStrToLocal for startTime construction"
      pattern: "dateStrToLocal"
    - from: "app/(app)/reports/_components/add-time-log-modal.tsx"
      to: "lib/utils/time.ts"
      via: "parseEstimate for h:mm → seconds conversion"
      pattern: "parseEstimate"
---

<objective>
Fix the saveTimeLog server action to accept duration = 0 and trigger cache revalidation, then create the AddTimeLogModal Client Component with a 3-field form (task combobox, date, duration).

Purpose: The mutation fix unblocks manual entries with no duration, and the modal is the primary user-facing form for the feature.
Output: Updated time-logs.ts + new add-time-log-modal.tsx ready to be mounted by the AddTimeLogButton island in plan 02.
</objective>

<execution_context>
@/Users/bogdan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bogdan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-manual-log-entry/01-CONTEXT.md
@.planning/phases/01-manual-log-entry/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts extracted from the codebase. Executor uses these directly. -->

From lib/actions/time-logs.ts (current — Task 1 modifies this):
```typescript
export async function saveTimeLog(data: {
  taskId: string
  startTime: Date
  duration: number // seconds
  type: "pomodoro" | "stopwatch"
})
// Current guard (line 83): if (data.duration < 1) return  ← CHANGE to < 0
// MISSING: revalidatePath("/", "layout") — MUST ADD after db.insert()
```

From lib/utils/tz.ts (already exists — use as-is):
```typescript
export function dateStrToLocal(str: string): Date
// Parses "YYYY-MM-DD", returns new Date(y, m-1, d, 12, 0, 0) — local noon
```

From lib/utils/time.ts (already exists — use as-is):
```typescript
export function parseEstimate(input: string): number | null
// Parses "1:30", "01:30" → seconds. Returns null for empty/invalid.
```

From app/(app)/_components/date-picker.tsx (already exists — reuse):
// Existing DatePicker component using react-day-picker v9.
// Props: { value: string; onChange: (dateStr: string) => void }
// (Confirm exact prop names by reading the file before implementing.)

From app/(app)/_components/task-modal.tsx (reference for modal structure):
// Pattern: fixed backdrop div bg-black/40 z-40, panel at z-50 with stopPropagation.
// For AddTimeLogModal use a centered panel max-w-md (simpler form, no mobile bottom-sheet needed).

Task shape needed by the modal (passed as prop from AddTimeLogButton in plan 02):
```typescript
interface Task {
  id: string
  title: string
}
```

AddTimeLogModal props (define these in the file):
```typescript
interface AddTimeLogModalProps {
  tasks: Task[]
  todayStr: string   // "YYYY-MM-DD" — initial value for date field
  onClose: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix saveTimeLog — accept duration = 0 and add revalidatePath</name>
  <files>lib/actions/time-logs.ts</files>
  <action>
Make two changes to `saveTimeLog` in `lib/actions/time-logs.ts`:

1. Change the duration guard on line 83 from `if (data.duration < 1) return` to `if (data.duration < 0) return`. This allows duration = 0 for manual entries where the user leaves the field blank.

2. Add `revalidatePath("/", "layout")` after the `db.insert(timeLogs).values(...)` call. This is currently missing — without it the Reports page will not refresh after a manual save. Import `revalidatePath` from `"next/cache"` (it is already imported at the top of the file for other actions).

Do not change any other function in the file.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>saveTimeLog guard reads `data.duration < 0` and `revalidatePath("/", "layout")` is called after insert. Build passes with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create AddTimeLogModal Client Component</name>
  <files>app/(app)/reports/_components/add-time-log-modal.tsx</files>
  <action>
Create `app/(app)/reports/_components/add-time-log-modal.tsx` as a `"use client"` component.

**Props interface:**
```typescript
interface Task { id: string; title: string }
interface AddTimeLogModalProps {
  tasks: Task[]
  todayStr: string   // "YYYY-MM-DD" initial date
  onClose: () => void
}
```

**Form state:**
- `selectedTask: Task | null` — starts null (required, cannot save without)
- `query: string` — combobox input text for filtering
- `dropdownOpen: boolean` — controls task list visibility
- `dateStr: string` — initialised to `todayStr` prop
- `durationStr: string` — empty string (optional field)
- `error: string | null` — inline validation message
- `isPending, startTransition` — from `useTransition()`

**Layout — modal structure (follow task-modal.tsx pattern):**
- Fixed backdrop: `<div className="fixed inset-0 bg-black/40 z-40" onClick={onClose} />`
- Panel: `<div className="fixed inset-0 flex items-center justify-center z-50 p-4" onClick={e => e.stopPropagation()}>` containing a white `div` with `className="bg-white rounded-xl shadow-xl w-full max-w-md p-6"`
- Title: `<h2 className="text-lg font-semibold text-gray-900 mb-4">Add report item entry</h2>` — exact text required
- Close button (×) top-right: `<button onClick={onClose} className="absolute top-4 right-4 text-gray-400 hover:text-gray-600">✕</button>`

**Field 1 — Task (searchable combobox, required):**
- Label: "Task"
- A `div` with `relative` for dropdown positioning
- Controlled `<input>` that shows `selectedTask.title` when a task is selected, or `query` when typing
- `onChange`: set `query`, clear `selectedTask`, open dropdown
- `onFocus`: open dropdown
- `onBlur`: close dropdown after a short delay (use `setTimeout(200ms)`) to allow `onMouseDown` to fire first
- Placeholder: `"Search tasks..."`
- Dropdown `<ul>` rendered when `dropdownOpen && filtered.length > 0`: absolute position, `bg-white rounded-xl border border-gray-200 shadow-xl max-h-48 overflow-y-auto z-10`
- Each `<li>` uses `onMouseDown` (not `onClick`) to set `selectedTask`, clear `query`, close dropdown
- Filter: `tasks.filter(t => t.title.toLowerCase().includes(query.toLowerCase()))`
- Styling: inputs use `"w-full text-sm border border-gray-200 rounded-lg px-3 py-2 outline-none focus:border-gray-400"`

**Field 2 — Date:**
- Label: "Date"
- Reuse the existing `DatePicker` component from `app/(app)/_components/date-picker.tsx`. Read that file first to confirm the exact prop names.
- Pass `value={dateStr}` and `onChange={(val) => setDateStr(val)}`

**Field 3 — Duration (optional):**
- Label: "Duration"
- Plain `<input type="text">` with `placeholder="h:mm (optional)"`, `value={durationStr}`, `onChange`
- Styling same as Task input

**Validation and submit:**
- "Save" button: disabled while `isPending`
- `handleSubmit()`:
  1. If `!selectedTask` → set error "Please select a task" and return
  2. If `durationStr.trim()` is non-empty: parse with `parseEstimate`. If null → set error "Duration must be h:mm format, e.g. 1:30" and return. Else `durationSeconds = parsed value`.
  3. If `durationStr.trim()` is empty: `durationSeconds = 0`
  4. `startTime = dateStrToLocal(dateStr)`
  5. Inside `startTransition(async () => { await saveTimeLog({ taskId: selectedTask.id, startTime, duration: durationSeconds, type: "stopwatch" }); onClose() })`
- Show `error` as `<p className="text-sm text-red-500 mt-1">{error}</p>` below the relevant field or above the Save button

**Imports to use:**
- `"use client"` at top
- `useState`, `useTransition` from `"react"`
- `saveTimeLog` from `"@/lib/actions/time-logs"`
- `DatePicker` from `"@/app/(app)/_components/date-picker"` (confirm exact path by checking the file)
- `parseEstimate` from `"@/lib/utils/time"`
- `dateStrToLocal` from `"@/lib/utils/tz"`

**Export:** `export function AddTimeLogModal({ tasks, todayStr, onClose }: AddTimeLogModalProps)`
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
File exists at app/(app)/reports/_components/add-time-log-modal.tsx. Build passes with no TypeScript errors. The component exports AddTimeLogModal and accepts tasks, todayStr, onClose props.
  </done>
</task>

</tasks>

<verification>
After both tasks: `pnpm build` passes with no errors. The modal component exists and the saveTimeLog guard is relaxed.
</verification>

<success_criteria>
- `lib/actions/time-logs.ts`: `saveTimeLog` guard is `data.duration < 0` and `revalidatePath("/", "layout")` is called after insert
- `app/(app)/reports/_components/add-time-log-modal.tsx` exists and exports `AddTimeLogModal`
- `pnpm build` produces no TypeScript or compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-manual-log-entry/01-01-SUMMARY.md`
</output>
